# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - main # Replace main with your default branch if necessary
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_RETOVERSE_78FF8 }} # Replace RETROVERSE_78FF8 with your project ID
          channelId: live
          projectId: retoverse-78ff8 # Replace RETROVERSE_78FF8 with your project ID
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
